---
- name: Run a shell command on target hosts
  hosts: localhost
  tasks:
    - name: Run 'uptime' command
      shell: ibmcloud login
      register: uptime_output

    - name: Print the output
      debug:
        msg: "{{ uptime_output.stdout }}"
